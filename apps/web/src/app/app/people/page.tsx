"use client";
import {useEffect,useState} from "react";import {Shell} from "../../../components/Shell";import {api} from "../../../lib/api";
type Employee={id:string;name:string;email:string;role:string;department:string;status:string;salary?:number|null};
export default function People(){const [items,setItems]=useState<Employee[]>([]);const [error,setError]=useState("");useEffect(()=>{api<{items:Employee[]}>("/employees",{},localStorage.getItem("movon_user")||undefined).then(x=>setItems(x.items)).catch(e=>setError(e.message))},[]);return <Shell><h1>Direktori Karyawan</h1>{error?<p className="notice">{error}. Masuk sebagai HR atau Manager untuk melihat direktori.</p>:<div className="card"><table className="table"><thead><tr><th>Nama</th><th>Departemen</th><th>Peran</th><th>Status</th><th>Gaji</th></tr></thead><tbody>{items.map(e=><tr key={e.id}><td><b>{e.name}</b><br/><span className="muted">{e.email}</span></td><td>{e.department}</td><td>{e.role}</td><td>{e.status}</td><td>{e.salary?new Intl.NumberFormat("id-ID",{style:"currency",currency:"IDR",maximumFractionDigits:0}).format(e.salary):"Tidak berwenang"}</td></tr>)}</tbody></table></div>}</Shell>}
