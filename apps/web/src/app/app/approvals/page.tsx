"use client";
import {useEffect,useState} from "react";import {Shell} from "../../../components/Shell";import {api} from "../../../lib/api";
type Request={id:string;employee_name:string;start:string;end:string;reason:string;status:string};
export default function Approvals(){const [items,setItems]=useState<Request[]>([]);const token=typeof window!=="undefined"?localStorage.getItem("movon_user")||undefined:undefined;const load=()=>api<{items:Request[]}>("/approvals",{},token).then(x=>setItems(x.items));useEffect(()=>{load()},[]);async function decide(id:string,decision:string){await api(`/approvals/${id}`,{method:"POST",body:JSON.stringify({decision,comment:"Sudah ditinjau"})},token);load()}return <Shell><h1>Persetujuan</h1><div className="card"><table className="table"><thead><tr><th>Karyawan</th><th>Tanggal</th><th>Alasan</th><th>Status</th><th>Aksi</th></tr></thead><tbody>{items.map(x=><tr key={x.id}><td>{x.employee_name}</td><td>{x.start} — {x.end}</td><td>{x.reason}</td><td><span className="status">{x.status}</span></td><td>{x.status==="pending"&&<div className="row"><button onClick={()=>decide(x.id,"approved")}>Setujui</button><button onClick={()=>decide(x.id,"rejected")}>Tolak</button></div>}</td></tr>)}</tbody></table></div></Shell>}
