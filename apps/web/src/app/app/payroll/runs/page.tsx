"use client";
import {useState} from "react";import {Shell} from "../../../../components/Shell";import {api} from "../../../../lib/api";
export default function Payroll(){const [data,setData]=useState<any>();const [error,setError]=useState("");async function calculate(){try{setData(await api("/payroll/runs",{method:"POST",body:JSON.stringify({month:"2026-09"})},localStorage.getItem("movon_user")||undefined))}catch(e){setError(e instanceof Error?e.message:"Gagal")}}return <Shell><h1>Payroll Runs</h1><p className="notice">Komponen PPh 21 dan BPJS adalah konfigurasi/manual pada MVP ini; hasil perlu ditinjau HR sebelum dipublikasikan.</p><button onClick={calculate}>Hitung payroll September 2026</button>{error&&<p className="notice">{error}</p>}{data&&<section className="card" style={{marginTop:16}}><h2>Draft {data.period}</h2><table className="table"><thead><tr><th>Karyawan</th><th>Bruto</th><th>Potongan</th><th>Net pay</th></tr></thead><tbody>{data.items.map((x:any)=><tr key={x.employee}><td>{x.employee}</td><td>{x.gross_rupiah.toLocaleString("id-ID")}</td><td>{x.deductions_rupiah.toLocaleString("id-ID")}</td><td><b>{x.net_rupiah.toLocaleString("id-ID")}</b></td></tr>)}</tbody></table></section>}</Shell>}
