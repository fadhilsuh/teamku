"use client";
import {useState} from "react";import {Shell} from "../../../../components/Shell";import {api} from "../../../../lib/api";
export default function TimeOff(){const [start,setStart]=useState("2026-09-10"),[end,setEnd]=useState("2026-09-10"),[reason,setReason]=useState("Keperluan keluarga"),[message,setMessage]=useState("");async function submit(){try{const r=await api<any>("/leave-requests",{method:"POST",body:JSON.stringify({start,end,reason})},localStorage.getItem("movon_user")||undefined);setMessage(`Permohonan ${r.status} berhasil dikirim.`)}catch(e){setMessage(e instanceof Error?e.message:"Gagal")}}return <Shell><h1>Ajukan cuti</h1><div className="card stack"><label>Tanggal mulai<input type="date" value={start} onChange={e=>setStart(e.target.value)}/></label><label>Tanggal selesai<input type="date" value={end} onChange={e=>setEnd(e.target.value)}/></label><label>Alasan<textarea value={reason} onChange={e=>setReason(e.target.value)}/></label><button onClick={submit}>Kirim untuk persetujuan</button>{message&&<p className="notice">{message}</p>}</div></Shell>}
