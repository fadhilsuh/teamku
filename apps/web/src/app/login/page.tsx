"use client";
import {useState} from "react"; import {useRouter} from "next/navigation"; import {api} from "../../lib/api";
export default function Login(){const [email,setEmail]=useState("employee@movon.test"),[error,setError]=useState("");const router=useRouter();async function submit(){try{const r=await api<{access_token:string}>("/auth/login",{method:"POST",body:JSON.stringify({email,password:"Demo123!"})});localStorage.setItem("movon_user",r.access_token);router.push("/app/overview")}catch(e){setError(e instanceof Error?e.message:"Gagal")}}return <main className="login-page"><section className="login-art"><div className="login-logo">mo<span>von</span></div><div><p className="eyebrow" style={{color:"#fff"}}>People operations, made human</p><h1>Presence with purpose.</h1><p>Kelola kehadiran, agenda harian, dan keputusan tim dalam satu ruang kerja yang tenang dan jelas.</p></div><p>PT Movon Solusi Kreatif · Jakarta, Indonesia</p></section><section className="login-form-wrap"><div className="login-form"><div className="brand">mo<span>von</span> HR</div><p className="eyebrow">Selamat datang kembali</p><h2>Masuk ke workspace Anda</h2><p className="muted">Gunakan akun kerja untuk melanjutkan.</p><div className="stack" style={{marginTop:32}}><label>Email kerja<input value={email} onChange={e=>setEmail(e.target.value)}/></label>{error&&<p className="notice">{error}</p>}<button onClick={submit}>Masuk ke Movon HR →</button></div><p className="login-demo"><b>Akun demo</b><br/>hr@movon.test · manager@movon.test · employee@movon.test<br/>Kata sandi: Demo123!</p></div></section></main>}
